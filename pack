# Needed for homebrew on linux
apt "build-essential"
apt "binutils"
apt "procps"
apt "curl"
apt "file"
apt "git"

brew "stow"

cask "iterm2"

apt "wezterm"

apt "neovim"

brew "gh"

uv "ruff"
uv "pyright"

cargo "zoxide"
stow "~/dotfiles/config_files"
shell "touch i_hope_this_works"
